package specs.fixtures

fun literalsDemo(): Unit {
    val name = "jv"
    val message = "Hello, ${name}!"

    val template = """
        Welcome, ${name}!
        Enjoy the platform.
    """

    val config = {
      "name": "jv",
      "enabled": true,
      "values": [1, 2, 3]
    }

    var runCount = 1
    var channel = "console"

    println(message)
    println("Direct literal output line")
    println("Inline interpolation: ${name}")
    println("Checkpoint ${runCount}: Welcome, ${name}!")
    println(runCount)
    println("Channel ${channel}")
    println(template)
    println(config)
}
