package com.example.demo

import java.util.List

fun int sum(values: Sequence<Int>) {
    return values.reduce(0) { acc value ->
        acc + value
    }
}

val description = when (sum([1 2 3])) {
    0 -> "empty"
    else -> "computed"
}
